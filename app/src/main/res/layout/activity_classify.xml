<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.bdxk.android.ui_android.ui.classify.ClassifyActivity">
    <TextView
        android:id="@+id/text"
        android:text="限制2个行业，每个行业3类资源"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <ListView
        android:id="@+id/leftList"
        android:scrollbars="none"
        android:layout_below="@+id/text"
        android:layout_width="100dp"
        android:layout_height="match_parent"/>
    <TextView
        android:id="@+id/tvInfo"
        android:text="点击可以多选，限制3个"
        android:padding="10dp"
        android:textColor="@android:color/white"
        android:background="@android:color/darker_gray"
        android:layout_below="@+id/text"
        android:layout_toEndOf="@+id/leftList"
        android:layout_toRightOf="@+id/leftList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <ListView
        android:id="@+id/rightList"
        android:divider="@android:color/transparent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/tvInfo"
        android:layout_toEndOf="@+id/leftList"
        android:layout_toRightOf="@+id/leftList"
        android:scrollbars="none" />
</RelativeLayout>
